import React from 'react';
import { Linking, StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { CreditCard, LockKeyhole } from 'lucide-react-native';
import { theme } from '../../../core/theme';

export const BookingPaymentPendingCard: React.FC<{ paymentUrl?: string }> = ({ paymentUrl }) => <View style={styles.card}><View style={styles.icon}><CreditCard size={19} color="#B45309" /></View><View style={styles.copy}><Text style={styles.title}>Paiement à finaliser</Text><Text style={styles.text}>{paymentUrl ? 'Finalisez votre paiement sécurisé pour envoyer la demande à l’hôte.' : 'Le lien de paiement est en cours de préparation. Actualisez dans quelques instants.'}</Text>{paymentUrl ? <TouchableOpacity onPress={() => Linking.openURL(paymentUrl)} style={styles.button}><Text style={styles.buttonText}>Reprendre le paiement</Text></TouchableOpacity> : null}</View><LockKeyhole size={16} color="#B45309" /></View>;
const styles = StyleSheet.create({ card: { flexDirection: 'row', alignItems: 'flex-start', gap: 10, padding: 15, borderRadius: 18, backgroundColor: '#FFFBEB', borderWidth: 1, borderColor: '#FDE68A' }, icon: { width: 36, height: 36, borderRadius: 11, backgroundColor: '#FEF3C7', alignItems: 'center', justifyContent: 'center' }, copy: { flex: 1 }, title: { color: '#92400E', fontFamily: theme.typography.fontFamily.displaySemiBold, fontSize: 15 }, text: { color: '#A16207', fontFamily: theme.typography.fontFamily.regular, fontSize: 12, lineHeight: 17, marginTop: 3 }, button: { alignSelf: 'flex-start', marginTop: 10, paddingHorizontal: 13, paddingVertical: 8, borderRadius: 16, backgroundColor: '#B45309' }, buttonText: { color: '#FFFFFF', fontFamily: theme.typography.fontFamily.bold, fontSize: 11 } });
